# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :ios, '8.0'

target 'CodePathDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CodePathDemo

  target 'CodePathDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CodePathDemoUITests' do
    # Pods for testing
  end
    pod 'SDWebImage', '~> 5.0'

end
